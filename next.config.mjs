/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'i.pinimg.com',
      'drive.google.com',
      'lh3.googleusercontent.com',
    ],
  },
};

export default nextConfig;
